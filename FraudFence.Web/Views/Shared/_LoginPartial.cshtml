<ul class="navbar-nav ml-auto">
    <li class="nav-item dropdown mx-2 no-arrow show">
        <a class="nav-link dropdown-toggle" href="#" id="userDropdown"
           role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <div>
                <div class="row">
                    <span class="col-12 mr-2 d-none d-lg-inline text-gray-800 medium text-right">@User.GetDisplayName()</span>
                    <span class="col-12 mr-2 d-none d-lg-inline text-gray-600 small text-right">@User.GetEmail()</span>
                </div>
            </div>

            <img class="img-profile rounded-circle ml-3"
                 src="~/lib/sb-admin-2/img/undraw_profile.svg">
        </a>

        <div class="dropdown-menu dropdown-menu-right shadow"
             aria-labelledby="userDropdown">
            <a class="dropdown-item" asp-controller="Account" asp-action="Profile">
                <i class="fas fa-user fa-sm fa-fw mr-2 text-gray-400"></i>
                Profile
            </a>

            <div class="dropdown-divider"></div>

            <form asp-area="" asp-controller="Accounts" asp-action="Logout" method="post" class="m-0 p-0">
                <button type="submit" class="dropdown-item">
                    <i class="fas fa-sign-out-alt fa-sm fa-fw mr-2 text-gray-400"></i>
                    Logout
                </button>
            </form>
        </div>
    </li>
</ul>
